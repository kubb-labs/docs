Toggle the export style and depth to see the generated barrels.

<BarrelTree />

Controls how the generated `index.ts` (barrel) re-exports the output. Defaults to `false` (no barrel), and also accepts `{ type: 'named' }`, `{ type: 'all' }`, or `{ nested: true }`.
